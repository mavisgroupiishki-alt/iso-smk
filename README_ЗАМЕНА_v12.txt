ИСПРАВЛЕНИЕ V12 — ЖЁЛТЫЕ ПОЛЯ ВНУТРИ САМИХ ДОКУМЕНТОВ

Заменить в GitHub только файл:
- docx_review.py

Что изменено:
- отдельный файл «00 ПРОВЕРКА ДАННЫХ ПЕРЕД ПОДАЧЕЙ.docx» больше не создаётся;
- сомнительное значение выделяется жёлтым непосредственно в той строке/ячейке,
  где оно находится в сформированном документе;
- если значение отсутствует, рядом с названием поля вставляется жёлтая надпись
  «ТРЕБУЕТ УТОЧНЕНИЯ»;
- обработка применяется к ISO, СУОТ, ISO+СУОТ, СПК Стройкомплекс, СПК БИСП,
  аттестации специалиста и аттестации компании;
- обрабатываются основной текст, таблицы, колонтитулы, сноски и примечания;
- подписи и декоративные пустые строки автоматически не закрашиваются.

После замены:
1. Commit changes.
2. Дождаться Deploy на Render.
3. Создать новый пакет документов.
Ранее сформированные DOCX автоматически не изменятся.
