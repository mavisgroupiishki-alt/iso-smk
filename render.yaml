services:
  - type: web
    name: iso-smk-generator
    env: python
    buildCommand: "pip install requests rarfile"
    startCommand: python server.py
    envVars:
      - key: VIBE_API_KEY
        sync: false
