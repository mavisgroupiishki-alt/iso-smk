services:
  - type: web
    name: iso-smk-generator
    env: python
    buildCommand: "apt-get install -y p7zip-full 2>/dev/null || true && pip install requests"
    startCommand: python server.py
    envVars:
      - key: VIBE_API_KEY
        sync: false
