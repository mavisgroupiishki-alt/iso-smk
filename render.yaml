services:
  - type: web
    name: iso-smk-generator
    env: python
    buildCommand: "pip install requests"
    startCommand: python server.py
    envVars:
      - key: VIBE_API_KEY
        sync: false
