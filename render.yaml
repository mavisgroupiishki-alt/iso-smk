services:
  - type: web
    name: iso-smk-generator
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: VIBE_API_KEY
        sync: false
      # Для постоянного серверного хранения подключите Render Persistent Disk
      # и задайте IGOR_DATA_DIR равным его mount path, например /data.
      - key: IGOR_DATA_DIR
        value: /data
